package com.example.car.repo1;

import com.example.car.model1.CarOptionValue;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CarOptionValueRepository extends JpaRepository<CarOptionValue, Integer> {
}